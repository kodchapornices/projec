<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>login</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #e8f4fb;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .login {
            background-color: #ffffff;
            padding: 20px 30px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
            width: 300px;
        }
        .login h1 {
            margin-bottom: 20px;
            color: #333333;
        }
        label {
            display: block; 
            text-align: left;
            font-weight: normal; 
            font-size: 15px;
        }
        input {
            width: 90%;
            padding: 10px;
            margin: 10px ;
            border: 1px solid #cccccc;
            border-radius: 4px;
        }
        .button {
            width: 50%;
            padding: 10px;
            background-color: #15addf9a;
            color: black;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            text-align: 10px;
            font-size: 15px;
        }
        .button:hover {
          background-color: #15addf9a;
        }
        .button:active {
          background-color: #15addf9a;
          box-shadow: 0 5px #666;
          transform: translateY(4px);
        }
        .link {
            display: block;
            margin-top: 10px;
            color: #007bff;
            text-decoration: none;
        }
        .link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="login">
        <form action="/users/login" method="post">
        <h1>เข้าสู่ระบบ</h1>
        <div>
            <label for="username">ชื่อผู้ใช้</label>
            <input type="text" id="username" placeholder="ชื่อผู้ใช้" name="username">
        </div>
        <div>
            <label for="password">รหัสผ่าน</label>
            <input type="password" id="password" placeholder="รหัสผ่าน" name="password">
        </div>
        <a href="forgotpassword" class="link">ลืมรหัสผ่าน</a>
        <button class="button">เข้าสู่ระบบ</button>
        <a href="register" class="link">สมัครสมาชิก</a>
    </form>
    </div>

</body>
</html>
